[project]
name = "pytest-workshop"
version = "1.0.0"

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
addopts = "--no-header"
junit_family = "xunit2"
junit_logging = "all"
markers = ["req_id: Requirement ID"]
